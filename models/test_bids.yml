version: 2

models:
  - name: test_bids              # имя файла без .sql
    description: "Сэмпл из public.bids_bid"
    columns:
      - name: id
        tests: [not_null, unique]
      - name: created
        tests: [not_null]
