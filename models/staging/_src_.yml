sources:
  - name: staging
    database: agroclub_db
    schema: public
    tables:
      - name: users_user
        freshness:
          warn_after:
            count: 10
            period: day
          error_after:
            count: 20
            period: day
        loaded_at_field: date_joined
